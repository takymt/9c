# 複数の変数を一括して扱う

- 初期化は `型 配列名[要素数]`(e.g., `int array[100]`)
- 初期値代入は `int array[100] = {0,1,2,3,4,5}`
  - 数値の個数は、配列の要素数以下(0埋めされる)
  - 要素数省略も可能 `int array[] = {0,1,2}`
- 配列はforループで便利
  - `sizeof` 関数でメモリ上のサイズがわかるので、`sizeof(array[100]) / sizeof(array[0])`