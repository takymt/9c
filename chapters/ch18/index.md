# マクロ機能

- `#define`マクロ
  - 数字の定数宣言は`#define VALUE 100`
  - 文字列の定数宣言も`#define NAME "name"`
- `const`宣言
  - 関数スコープ中でのみ必要な定数や、関数にreadonly引数として渡したい時に使用
  - 読み取り専用になるので、`const char *name = "..."`のようなポインタ定義と相性がいい
- `enum`定数
  - 自動で整数値0,...に対応する定数を定義する